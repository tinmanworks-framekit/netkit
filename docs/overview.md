# NetKit Overview

NetKit provides optional transport implementations that can be linked when multiprocess orchestration needs IPC.

Current baseline includes:
- shared-memory transport skeleton
- control-channel backend skeleton
- ShmPipe factory composition
